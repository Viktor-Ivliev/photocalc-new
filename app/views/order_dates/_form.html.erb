<%= form_for(@order_date) do |f| %>
  <% if @order_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_date.errors.count, "error") %> prohibited this order_date from being saved:</h2>

      <ul>
      <% @order_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_area :date %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
